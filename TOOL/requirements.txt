litellm>=1.40.0
python-dotenv>=1.0.0
pydantic>=2.0.0
streamlit>=1.30.0
requests>=2.31.0
markdown>=3.5.0
xhtml2pdf>=0.2.16
weasyprint>=61.0

# Note: sqlite3 is Python built-in, no install needed.
# Firebase removed for demo (using SQLite instead). To re-enable:
#   pip install firebase-admin>=6.0.0
